# Actualmente el pipeline no requiere variables de entorno.
# Se eliminó la dependencia de OpenAI (ya no hay ranking con IA).
# Este archivo se deja como placeholder para configuración futura
# (ej. credenciales de login para scrapers, webhooks de notificación, etc.)